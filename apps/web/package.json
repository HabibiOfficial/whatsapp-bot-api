{
  "name": "@whatsapp-bot-api/web",
  "version": "1.0.0",
  "description": "Public REST API gateway + landing page (Vercel serverless).",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@whatsapp-bot-api/db": "*"
  }
}
